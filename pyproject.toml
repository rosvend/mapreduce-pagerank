[project]
name = "python-pagerank-mapreduce"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
demo = [
    "matplotlib>=3.11.1",
    "neo4j>=6.3.0",
    "networkx>=3.6.1",
]
